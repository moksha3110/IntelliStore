apiVersion: v1
kind: Service
metadata:
  name: metadata-service
  namespace: intellistore
spec:
  selector:
    app: metadata-service
  ports:
    - port: 4002
      targetPort: 4002
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: metadata-service
  namespace: intellistore
spec:
  replicas: 1
  selector:
    matchLabels:
      app: metadata-service
  template:
    metadata:
      labels:
        app: metadata-service
    spec:
      # Disable Kubernetes' legacy Docker-link service env vars: for a Service
      # named e.g. auth-service, k8s would otherwise inject AUTH_SERVICE_PORT=
      # tcp://<clusterIP>:4001, colliding with our own AUTH_SERVICE_PORT (the
      # numeric listen port). We use in-cluster DNS for discovery, so these
      # link vars are unnecessary anyway.
      enableServiceLinks: false
      containers:
        - name: metadata-service
          image: intellistore/metadata-service:local
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 4002
          envFrom:
            - configMapRef:
                name: intellistore-config
            - secretRef:
                name: intellistore-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 4002
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 4002
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 50m
              memory: 96Mi
            limits:
              memory: 256Mi
