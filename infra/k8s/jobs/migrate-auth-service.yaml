apiVersion: batch/v1
kind: Job
metadata:
  name: migrate-auth-service
  namespace: intellistore
spec:
  backoffLimit: 10
  ttlSecondsAfterFinished: 300
  template:
    metadata:
      labels:
        app: migrate-auth-service
    spec:
      restartPolicy: Never
      enableServiceLinks: false
      containers:
        - name: migrate
          image: intellistore/auth-service:local
          imagePullPolicy: IfNotPresent
          command: ["node", "services/auth-service/dist/db/migrate.js"]
          envFrom:
            - configMapRef:
                name: intellistore-config
            - secretRef:
                name: intellistore-secrets
