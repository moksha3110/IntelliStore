apiVersion: batch/v1
kind: Job
metadata:
  name: migrate-replication-service
  namespace: intellistore
spec:
  backoffLimit: 10
  ttlSecondsAfterFinished: 300
  template:
    metadata:
      labels:
        app: migrate-replication-service
    spec:
      restartPolicy: Never
      enableServiceLinks: false
      containers:
        - name: migrate
          image: intellistore/replication-service:local
          imagePullPolicy: IfNotPresent
          command: ["node", "services/replication-service/dist/db/migrate.js"]
          envFrom:
            - configMapRef:
                name: intellistore-config
            - secretRef:
                name: intellistore-secrets
