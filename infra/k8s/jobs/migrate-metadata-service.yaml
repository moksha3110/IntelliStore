apiVersion: batch/v1
kind: Job
metadata:
  name: migrate-metadata-service
  namespace: intellistore
spec:
  backoffLimit: 10
  ttlSecondsAfterFinished: 300
  template:
    metadata:
      labels:
        app: migrate-metadata-service
    spec:
      restartPolicy: Never
      enableServiceLinks: false
      containers:
        - name: migrate
          image: intellistore/metadata-service:local
          imagePullPolicy: IfNotPresent
          command: ["node", "services/metadata-service/dist/db/migrate.js"]
          envFrom:
            - configMapRef:
                name: intellistore-config
            - secretRef:
                name: intellistore-secrets
